[tools]
python = "3.11"
node = "22"
uv = "latest"
just = "latest"

[tasks.setup]
description = "Install backend and frontend dependencies"
run = [
  "just setup",
]

[tasks.check]
description = "Run main local checks"
run = [
  "just check",
]
