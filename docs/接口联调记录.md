# 接口联调记录

## 联调日期

2026-04-28

## 当前联调环境

当前机器未检测到 Docker 和本地 PostgreSQL 服务，因此本次后端接口联调使用项目内 SQLite 开发库：

```text
DATABASE_URL=sqlite:///dev.db
```

SQLite 数据库文件位置：

```text
backend/instance/dev.db
```

后端依赖由于虚拟环境 `ensurepip` 被临时目录权限限制，已安装到：

```text
backend/.python_packages
```

该目录已加入 `.gitignore`，只作为本地运行产物。

## 已完成操作

1. 安装后端依赖。
2. 执行数据库初始化。
3. 执行种子数据脚本。
4. 启动 Flask 后端服务并通过 HTTP 调用接口。
5. 验证健康检查、注册、登录、赛事列表主链路。
6. 执行后端最小测试用例。

## 验证结果

### 健康检查

接口：

```text
GET /api/v1/health
```

结果：

```text
code=0
status=ok
```

### 注册

接口：

```text
POST /api/v1/auth/register
```

结果：

```text
code=0
注册用户=demo20260428210930
```

### 登录

接口：

```text
POST /api/v1/auth/login
```

结果：

```text
code=0
登录用户=demo20260428210930
access_token 正常返回
```

### 赛事列表

接口：

```text
GET /api/v1/competitions?page=1&page_size=10
```

结果：

```text
code=0
total=2
第一条赛事=蓝桥杯全国软件和信息技术专业人才大赛
```

### 后端测试

命令：

```text
python -m pytest -q
```

结果：

```text
1 passed
```

## 说明

本次已经跑通后端主链路。当前没有保留后台服务进程常驻；需要手动启动时，可执行：

```powershell
python scripts\run_backend_dev.py
```

如果需要切换到 PostgreSQL，只需启动 PostgreSQL 服务并将 `DATABASE_URL` 改为 PostgreSQL 连接字符串，再重新执行初始化和种子脚本。
